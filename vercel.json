{
  "version": 2,
  "builds": [
    {
      "src": "VideoApp/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/VideoApp/dist/$1" }
  ]
}